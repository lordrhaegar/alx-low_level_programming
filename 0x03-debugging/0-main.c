#include <stdio.h>
#include "main.h"

/**
 * main - the entry point of the program
 * Description - Check for positive or negative integers
 * Return: Always 0.
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
