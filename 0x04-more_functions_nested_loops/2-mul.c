#include <stdio.h>
#include "main.h"

/**
 * mul - multipply numbers
 * Description - function to multiply 2 numbers
 * @a: first number
 * @b: second number
 * Return - returns the product
 */
int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
